using UnityEngine;
using System.Collections;

public class playerBlock : playerController {

	// Use this for initialization
	void awake () {
		
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetKeyDown("alt")){

		}
	}
}
